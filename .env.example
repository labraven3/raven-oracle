# Raven Oracle local development environment
# Copy this file to .env for local-only settings. Never commit real .env files.

# PostgreSQL connection placeholder. Prisma and migrations are not configured yet.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/raven_oracle"

# Backend API settings
PORT=4000
WEB_ORIGIN="http://localhost:3000"
