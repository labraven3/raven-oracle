"use client";

import { useCallback, useEffect, useMemo, useState } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
import SiteHeader from "../../components/SiteHeader";
import { API_BASE_URL } from "@/lib/api-config";

type User = { id: string; email: string | null; emailVerifiedAt?: string | null; displayName?: string | null; username?: string | null; role?: string; status?: string };
type Social = { id: string; provider: "X" | "DISCORD"; providerUsername?: string | null; displayName?: string | null };
type TelegramAccount = { telegramUserId: string; username?: string | null; firstName?: string | null; lastName?: string | null; connectedAt?: string | null };
type Wallet = { id: string; address: string; chain: string; network: string; label?: string | null; status?: string; isPrimary?: boolean };
type ChainFamily = "EVM" | "SOLANA" | "APTOS" | "SUI" | "CARDANO" | "BITCOIN" | "COSMOS" | "RIPPLE" | "TRON" | "TON" | "VENOM" | "TEZOS" | "MULTIVERSX" | "NEAR" | "HEDERA" | "FLOW" | "REEF" | "STARKNET";
type ChainOption = { id: string; name: string; family: ChainFamily; icon: string };

const CHAIN_OPTIONS: ChainOption[] = [
  { id: "ethereum", name: "Ethereum", family: "EVM", icon: "https://atlas3.io/images/network_ethereum.svg" }, { id: "solana", name: "Solana", family: "SOLANA", icon: "https://atlas3.io/images/network_solana.svg" }, { id: "polygon", name: "Polygon", family: "EVM", icon: "https://atlas3.io/images/network_polygon.svg" }, { id: "aptos", name: "Aptos", family: "APTOS", icon: "https://atlas3.io/images/network_aptos.svg" }, { id: "sui", name: "Sui", family: "SUI", icon: "https://atlas3.io/images/network_sui.svg" },
  { id: "cardano", name: "Cardano", family: "CARDANO", icon: "https://atlas3.io/images/network_cardano.svg" }, { id: "bitcoin", name: "Bitcoin", family: "BITCOIN", icon: "https://atlas3.io/images/network_bitcoin.svg" }, { id: "avax", name: "Avax", family: "EVM", icon: "https://atlas3.io/images/network_avax.svg" }, { id: "venom", name: "Venom", family: "VENOM", icon: "https://atlas3.io/images/network_venom.svg" }, { id: "injective", name: "Injective", family: "COSMOS", icon: "https://atlas3.io/images/network_injective.svg" },
  { id: "sei", name: "Sei", family: "COSMOS", icon: "https://atlas3.io/images/network_sei.svg" }, { id: "base", name: "Base", family: "EVM", icon: "https://atlas3.io/images/network_base.svg" }, { id: "ripple", name: "Ripple", family: "RIPPLE", icon: "https://atlas3.io/images/network_ripple.svg" }, { id: "arbitrum", name: "Arbitrum", family: "EVM", icon: "https://atlas3.io/images/network_arbitrum.svg" }, { id: "immutable", name: "Immutable", family: "EVM", icon: "https://atlas3.io/images/network_immutable.svg" },
  { id: "flow", name: "Flow", family: "FLOW", icon: "https://atlas3.io/images/network_flow.svg" }, { id: "binance", name: "Binance", family: "EVM", icon: "https://atlas3.io/images/network_binance.svg" }, { id: "tezos", name: "Tezos", family: "TEZOS", icon: "https://atlas3.io/images/network_tezos.svg" }, { id: "multiversx", name: "MultiversX", family: "MULTIVERSX", icon: "https://atlas3.io/images/network_multiversx.svg" }, { id: "near", name: "Near", family: "NEAR", icon: "https://atlas3.io/images/network_near.svg" },
  { id: "hedera", name: "Hedera", family: "HEDERA", icon: "https://atlas3.io/images/network_hedera.svg" }, { id: "cosmos", name: "Cosmos", family: "COSMOS", icon: "https://atlas3.io/images/network_cosmos.svg" }, { id: "reef", name: "Reef", family: "REEF", icon: "https://atlas3.io/images/network_reef.svg" }, { id: "starknet", name: "Starknet", family: "STARKNET", icon: "https://atlas3.io/images/network_starknet.svg" }, { id: "manta", name: "Manta", family: "EVM", icon: "https://atlas3.io/images/network_manta.svg" },
  { id: "monad", name: "Monad", family: "EVM", icon: "https://atlas3.io/images/network_monad.svg" }, { id: "blast", name: "Blast", family: "EVM", icon: "https://atlas3.io/images/network_blast.svg" }, { id: "stargaze", name: "Stargaze", family: "COSMOS", icon: "https://atlas3.io/images/network_stargaze.svg" }, { id: "scroll", name: "Scroll", family: "EVM", icon: "https://atlas3.io/images/network_scroll.svg" }, { id: "zksync", name: "zkSync", family: "EVM", icon: "https://atlas3.io/images/network_zksync.svg" },
  { id: "enjin", name: "Enjin", family: "EVM", icon: "https://atlas3.io/images/network_enjin.svg" }, { id: "linea", name: "Linea", family: "EVM", icon: "https://atlas3.io/images/network_linea.svg" }, { id: "oraichain", name: "Oraichain", family: "COSMOS", icon: "https://atlas3.io/images/network_oraichain.svg" }, { id: "ton", name: "TON", family: "TON", icon: "https://atlas3.io/images/network_ton.svg" }, { id: "viction", name: "Viction", family: "EVM", icon: "https://atlas3.io/images/network_viction.svg" },
  { id: "bera", name: "Bera", family: "EVM", icon: "https://atlas3.io/images/network_bera.svg" }, { id: "tron", name: "Tron", family: "TRON", icon: "https://atlas3.io/images/network_tron.svg" }, { id: "apechain", name: "ApeChain", family: "EVM", icon: "https://atlas3.io/images/network_apechain.svg" }, { id: "abstract", name: "Abstract", family: "EVM", icon: "https://atlas3.io/images/network_abstract.svg" }, { id: "hyperliquid", name: "Hyperliquid", family: "EVM", icon: "https://atlas3.io/images/network_hyperliquid.svg" },
  { id: "story", name: "Story", family: "EVM", icon: "https://atlas3.io/images/network_story.svg" }, { id: "xion", name: "XION", family: "COSMOS", icon: "https://atlas3.io/images/network_xion.svg" }, { id: "somnia", name: "Somnia", family: "EVM", icon: "https://atlas3.io/images/network_somnia.svg" }, { id: "sophon", name: "Sophon", family: "EVM", icon: "https://atlas3.io/images/network_sophon.svg" }, { id: "robinhood", name: "Robinhood", family: "EVM", icon: "https://atlas3.io/images/network_robinhood.svg" },
];
const CHAIN_MAP = new Map(CHAIN_OPTIONS.map((chain) => [chain.id, chain]));
async function api<T>(path: string, options: RequestInit = {}) { const headers = new Headers(options.headers); headers.set("Content-Type", "application/json"); const token = typeof window !== "undefined" ? localStorage.getItem("raven_token") : null; if (token) headers.set("Authorization", `Bearer ${token}`); const response = await fetch(`${API_BASE_URL}${path}`, { ...options, headers, credentials: "include" }); const data = await response.json().catch(() => ({})); if (!response.ok) throw new Error(data.message ?? `Request failed (${response.status})`); return data as T; }
function shortenAddress(address: string) { return address.length <= 16 ? address : `${address.slice(0, 6)}...${address.slice(-6)}`; }
function chainForWallet(wallet: Wallet) { return CHAIN_MAP.get(wallet.network) ?? (wallet.network === "ethereum-mainnet" ? CHAIN_MAP.get("ethereum") : wallet.network === "solana-mainnet" ? CHAIN_MAP.get("solana") : undefined); }
function addressPlaceholder(chain: ChainOption) { switch (chain.family) { case "EVM": return "0x… wallet address"; case "SOLANA": return "Solana wallet address"; case "APTOS": return "0x… Aptos address"; case "SUI": return "0x… 64-character Sui address"; case "CARDANO": return "addr1… Cardano address"; case "BITCOIN": return "bc1… Bitcoin address"; case "COSMOS": return `${chain.name.toLowerCase()}1… address`; case "RIPPLE": return "r… Ripple address"; case "TRON": return "T… Tron address"; case "TON": return "EQ… or UQ… TON address"; case "VENOM": return "0:… or EQ… Venom address"; case "TEZOS": return "tz1… / tz2… / tz3… address"; case "MULTIVERSX": return "erd1… MultiversX address"; case "NEAR": return "yourname.near or implicit account"; case "HEDERA": return "0.0.x Hedera account"; case "FLOW": return "0x… Flow address"; case "REEF": return "5… Reef address"; case "STARKNET": return "0x… Starknet address"; default: return "Wallet address"; } }
function isCompatibleAddress(address: string, family: ChainFamily) { const value = address.trim(); if (!value) return false; switch (family) { case "EVM": return /^0x[0-9a-fA-F]{40}$/.test(value); case "SOLANA": return value.length >= 32 && value.length <= 44 && /^[1-9A-HJ-NP-Za-km-z]+$/.test(value); case "APTOS": return /^0x[0-9a-fA-F]{1,64}$/.test(value); case "SUI": case "STARKNET": return /^0x[0-9a-fA-F]{64}$/.test(value); case "CARDANO": return /^(addr1|stake1)[0-9a-z]{20,120}$/.test(value); case "BITCOIN": return /^(bc1|[13])[a-zA-HJ-NP-Z0-9]{20,90}$/i.test(value); case "COSMOS": return /^[a-z0-9]+1[0-9a-z]{20,90}$/.test(value); case "RIPPLE": return /^r[1-9A-HJ-NP-Za-km-z]{24,34}$/.test(value); case "TRON": return /^T[1-9A-HJ-NP-Za-km-z]{33}$/.test(value); case "TON": return /^[EU]Q[A-Za-z0-9_-]{46}$/.test(value); case "VENOM": return /^(0|-1):[0-9a-fA-F]{64}$/.test(value) || /^[EU]Q[A-Za-z0-9_-]{46}$/.test(value); case "TEZOS": return /^(tz[1-3]|KT1)[1-9A-HJ-NP-Za-km-z]{30,36}$/.test(value); case "MULTIVERSX": return /^erd1[0-9a-z]{58}$/.test(value); case "NEAR": return /^[a-f0-9]{64}$/.test(value) || /^[a-z0-9._-]{2,64}$/.test(value); case "HEDERA": return /^0\.0\.\d+$/.test(value) || /^0x[0-9a-fA-F]{40}$/.test(value); case "FLOW": return /^0x[0-9a-fA-F]{16}$/.test(value); case "REEF": return /^5[1-9A-HJ-NP-Za-km-z]{45,50}$/.test(value); default: return false; } }
function SocialIcon({ provider }: { provider: "X" | "DISCORD" | "TELEGRAM" }) { if (provider === "X") return <span className="text-lg font-semibold text-black">𝕏</span>; if (provider === "DISCORD") return <span className="text-sm font-black text-[#5865f2]">◉</span>; return <span className="text-base text-sky-500">➤</span>; }

export default function AccountPage() {
  const router = useRouter();
  const [user, setUser] = useState<User | null>(null); const [socials, setSocials] = useState<Social[]>([]); const [telegramAccount, setTelegramAccount] = useState<TelegramAccount | null>(null); const [wallets, setWallets] = useState<Wallet[]>([]); const [username, setUsername] = useState(""); const [displayName, setDisplayName] = useState(""); const [address, setAddress] = useState(""); const [selectedChainId, setSelectedChainId] = useState("ethereum"); const [chainModalOpen, setChainModalOpen] = useState(false); const [telegramOpen, setTelegramOpen] = useState(false); const [editing, setEditing] = useState(false); const [busy, setBusy] = useState(false); const [message, setMessage] = useState(""); const [error, setError] = useState("");
  const selectedChain = CHAIN_MAP.get(selectedChainId) ?? CHAIN_OPTIONS[0]!;
  const load = useCallback(async () => { const me = await api<{ user: User }>("/auth/me"); setUser(me.user); setUsername(me.user.username ?? ""); setDisplayName(me.user.displayName ?? ""); const [socialResult, walletResult, telegramResult] = await Promise.all([api<{ accounts: Social[] }>("/social-accounts/"), api<{ wallets: Wallet[] }>("/wallets/"), api<{ account: TelegramAccount | null }>("/auth/telegram/status")]); setSocials(socialResult.accounts); setWallets(walletResult.wallets.filter((wallet) => wallet.status !== "ARCHIVED" && wallet.status !== "DELETED")); setTelegramAccount(telegramResult.account); }, []);
  useEffect(() => { const hash = new URLSearchParams(window.location.hash.replace(/^#/, "")); const callbackToken = hash.get("token"); if (callbackToken) { localStorage.setItem("raven_token", callbackToken); window.history.replaceState(null, "", `${window.location.pathname}${window.location.search}`); } void load().catch(() => { localStorage.removeItem("raven_token"); router.replace("/login?next=/account"); }); }, [load, router]);
  const updateProfile = async () => { setBusy(true); setError(""); setMessage(""); try { await api("/profile", { method: "PATCH", body: JSON.stringify({ username: username.trim() || undefined, displayName: displayName.trim() || undefined }) }); await load(); setEditing(false); setMessage("Profile updated successfully."); } catch (e) { setError(e instanceof Error ? e.message : "Unable to update profile"); } finally { setBusy(false); } };
  const reconnectSocial = async (provider: "x" | "discord") => { setBusy(true); setError(""); setMessage(""); try { const data = await api<{ authorizationUrl: string }>(`/auth/${provider}/start`); window.location.assign(data.authorizationUrl); } catch (e) { setError(e instanceof Error ? e.message : `Unable to reconnect ${provider}`); setBusy(false); } };
  const disconnectSocial = async (id: string) => { if (!window.confirm("Disconnect this social account?")) return; setBusy(true); setError(""); try { await api(`/social-accounts/${id}`, { method: "DELETE" }); await load(); setMessage("Social account disconnected."); } catch (e) { setError(e instanceof Error ? e.message : "Unable to disconnect account"); } finally { setBusy(false); } };
  const connectTelegram = async () => { setBusy(true); setError(""); setMessage(""); try { const data = await api<{ authorizationUrl: string }>("/auth/telegram/start"); window.location.assign(data.authorizationUrl); } catch (e) { setError(e instanceof Error ? e.message : "Telegram connection is unavailable"); setBusy(false); } };
  const reconnectTelegram = connectTelegram;
  const disconnectTelegram = async () => { if (!window.confirm("Disconnect your Telegram account?")) return; setBusy(true); setError(""); try { await api("/auth/telegram/disconnect", { method: "DELETE" }); setTelegramAccount(null); setMessage("Telegram account disconnected."); } catch (e) { setError(e instanceof Error ? e.message : "Unable to disconnect Telegram"); } finally { setBusy(false); } };
  const addWallet = async () => { const trimmed = address.trim(); if (!isCompatibleAddress(trimmed, selectedChain.family)) { setError(`Enter a valid ${selectedChain.name} wallet address.`); return; } setBusy(true); setError(""); setMessage(""); try { await api("/wallets/", { method: "POST", body: JSON.stringify({ address: trimmed, chain: selectedChain.family === "SOLANA" ? "SOLANA" : "EVM", network: selectedChain.id }) }); setAddress(""); setChainModalOpen(false); await load(); setMessage(`${selectedChain.name} wallet added successfully.`); } catch (e) { setError(e instanceof Error ? e.message : "Unable to add wallet"); } finally { setBusy(false); } };
  const removeWallet = async (id: string) => { if (!window.confirm("Remove this wallet from your account?")) return; setBusy(true); setError(""); try { await api(`/wallets/${id}`, { method: "DELETE" }); await load(); setMessage("Wallet removed."); } catch (e) { setError(e instanceof Error ? e.message : "Unable to remove wallet"); } finally { setBusy(false); } };
  const makePrimary = async (id: string) => { setBusy(true); setError(""); try { await api(`/wallets/${id}`, { method: "PATCH", body: JSON.stringify({ isPrimary: true }) }); await load(); setMessage("Primary wallet updated."); } catch (e) { setError(e instanceof Error ? e.message : "Unable to update primary wallet"); } finally { setBusy(false); } };
  const logout = async () => { try { await api("/auth/logout", { method: "POST" }); } catch {} finally { localStorage.removeItem("raven_token"); router.replace("/"); } };
  const discord = socials.find((social) => social.provider === "DISCORD"); const xSocial = socials.find((social) => social.provider === "X"); const primaryWallets = useMemo(() => wallets.filter((wallet) => wallet.isPrimary), [wallets]); const otherWallets = useMemo(() => wallets.filter((wallet) => !wallet.isPrimary), [wallets]);
  if (!user) return <main className="min-h-screen bg-slate-50 text-slate-900 dark:bg-[#07070a] dark:text-zinc-100"><SiteHeader /><section className="mx-auto max-w-lg px-5 py-24 text-center"><div className="mx-auto h-10 w-10 animate-spin rounded-full border-2 border-violet-500 border-t-transparent" /><p className="mt-5 text-sm text-slate-500 dark:text-zinc-500">Loading your profile…</p></section></main>;
  const initials = (displayName || username || user.email || "R").trim().slice(0, 1).toUpperCase();
  return <main className="min-h-screen bg-slate-50 text-slate-900 dark:bg-[#07070a] dark:text-zinc-100"><SiteHeader /><div className="mx-auto max-w-7xl px-4 py-5 sm:px-6 lg:px-8"><section className="min-w-0 bg-white px-4 py-5 dark:bg-[#0d0c11] sm:px-7 lg:px-8"><div className="flex items-center justify-between border-b border-slate-200 pb-5 dark:border-white/10"><div className="flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-full bg-violet-600 text-base font-black text-white">{initials}</div><div><h1 className="text-sm font-bold text-slate-900 dark:text-zinc-100">Welcome back, {username || displayName || "Raven"}</h1><p className="text-[10px] text-slate-500 dark:text-zinc-500">Manage your account, socials &amp; wallets</p></div></div><button onClick={() => void logout()} className="rounded-lg border border-red-400 px-4 py-2 text-[10px] font-bold text-red-500 hover:bg-red-50 dark:hover:bg-red-500/10">Log out</button></div>
      {(error || message) && <div className="mt-5 space-y-2">{error && <div className="rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-xs text-red-600 dark:border-red-900/40 dark:bg-red-950/20 dark:text-red-300">{error}</div>}{message && <div className="rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-xs text-emerald-600 dark:border-emerald-900/40 dark:bg-emerald-950/20 dark:text-emerald-300">{message}</div>}</div>}
      <div className="mt-6 rounded-xl border border-slate-200 bg-white p-5 shadow-[0_2px_10px_rgba(15,23,42,0.04)] dark:border-white/10 dark:bg-[#0d0c11]"><div className="flex items-center justify-between"><div><h2 className="text-sm font-bold">Profile Information</h2><p className="mt-1 text-[10px] text-slate-500 dark:text-zinc-500">Update your personal details and how others see you.</p></div>{!editing && <button onClick={() => setEditing(true)} className="rounded-lg bg-violet-600 px-4 py-2 text-[10px] font-bold text-white">Edit</button>}</div><div className="mt-5 grid gap-4 md:grid-cols-2"><div className="md:col-span-2"><label className="text-[10px] font-bold text-slate-700 dark:text-zinc-300">Email address</label><div className="mt-1 flex items-center justify-between rounded-lg border border-slate-200 bg-slate-50 px-3 py-2.5 dark:border-white/10 dark:bg-black/20"><span className="min-w-0 truncate text-xs text-slate-600 dark:text-zinc-400">{user.email || "—"}</span><span className={`ml-3 shrink-0 rounded-md px-2 py-1 text-[9px] font-bold ${user.emailVerifiedAt ? "bg-emerald-100 text-emerald-700 dark:bg-emerald-500/15 dark:text-emerald-300" : "bg-amber-100 text-amber-700 dark:bg-amber-500/15 dark:text-amber-300"}`}>{user.emailVerifiedAt ? "✓ Verified" : "Not verified"}</span></div></div><div><label className="text-[10px] font-bold text-slate-700 dark:text-zinc-300">Username</label>{editing ? <input value={username} onChange={(event) => setUsername(event.target.value)} maxLength={32} className="mt-1 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-xs dark:border-white/10 dark:bg-black/20" /> : <div className="mt-1 rounded-lg border border-slate-200 px-3 py-2.5 text-xs text-slate-700 dark:border-white/10 dark:text-zinc-300">{username || "Not set"}</div>}</div><div><label className="text-[10px] font-bold text-slate-700 dark:text-zinc-300">Display name</label>{editing ? <input value={displayName} onChange={(event) => setDisplayName(event.target.value)} maxLength={80} className="mt-1 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-xs dark:border-white/10 dark:bg-black/20" /> : <div className="mt-1 rounded-lg border border-slate-200 px-3 py-2.5 text-xs text-slate-700 dark:border-white/10 dark:text-zinc-300">{displayName || "Not set"}</div>}</div></div>{editing && <div className="mt-4 flex justify-end gap-2"><button onClick={() => { setEditing(false); setUsername(user.username ?? ""); setDisplayName(user.displayName ?? ""); }} className="rounded-lg border border-slate-200 px-4 py-2 text-[10px] font-bold text-slate-600 dark:border-white/10 dark:text-zinc-300">Cancel</button><button disabled={busy} onClick={() => void updateProfile()} className="rounded-lg bg-violet-600 px-4 py-2 text-[10px] font-bold text-white">Save changes</button></div>}</div>
      <div className="mt-5 rounded-xl border border-slate-200 bg-white p-5 shadow-[0_2px_10px_rgba(15,23,42,0.04)] dark:border-white/10 dark:bg-[#0d0c11]"><h2 className="text-sm font-bold">Social accounts</h2><p className="mt-1 text-[10px] text-slate-500 dark:text-zinc-500">Verify your identity so we can easily message you about a giveaway or allowlist.</p><div className="mt-4 space-y-2"><div className="flex flex-col gap-3 rounded-lg border border-slate-200 px-3 py-3 dark:border-white/10 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><div className="flex h-8 w-8 items-center justify-center rounded-full border border-slate-200 dark:border-white/10"><SocialIcon provider="DISCORD" /></div><div><div className="text-[10px] font-bold">Discord account</div><div className="text-[10px] text-slate-500 dark:text-zinc-500">{discord?.providerUsername || "Not connected"}</div></div></div><div className="flex gap-2"><button disabled={busy} onClick={() => void reconnectSocial("discord")} className="rounded-lg border border-violet-300 px-3 py-2 text-[10px] font-bold text-violet-700 dark:border-violet-400/40 dark:text-violet-300">{discord ? "Relink" : "Connect"}</button>{discord && <button disabled={busy} onClick={() => void disconnectSocial(discord.id)} className="rounded-lg border border-red-300 px-3 py-2 text-[10px] font-bold text-red-500 dark:border-red-400/30">Unlink</button>}</div></div><div className="flex flex-col gap-3 rounded-lg border border-slate-200 px-3 py-3 dark:border-white/10 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><div className="flex h-8 w-8 items-center justify-center rounded-full border border-slate-200 dark:border-white/10"><SocialIcon provider="X" /></div><div><div className="text-[10px] font-bold">X account</div><div className="text-[10px] text-slate-500 dark:text-zinc-500">{xSocial?.providerUsername ? `@${xSocial.providerUsername}` : "Not connected"}</div></div></div><div className="flex gap-2"><button disabled={busy} onClick={() => void reconnectSocial("x")} className="rounded-lg border border-violet-300 px-3 py-2 text-[10px] font-bold text-violet-700 dark:border-violet-400/40 dark:text-violet-300">{xSocial ? "Reconnect" : "Connect X"}</button>{xSocial && <button disabled={busy} onClick={() => void disconnectSocial(xSocial.id)} className="rounded-lg border border-red-300 px-3 py-2 text-[10px] font-bold text-red-500 dark:border-red-400/30">Disconnect</button>}</div></div><div className="flex flex-col gap-3 rounded-lg border border-slate-200 px-3 py-3 dark:border-white/10 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><div className="flex h-8 w-8 items-center justify-center rounded-full border border-slate-200 dark:border-white/10"><SocialIcon provider="TELEGRAM" /></div><div><div className="text-[10px] font-bold">Telegram account</div><div className="text-[10px] text-slate-500 dark:text-zinc-500">{telegramAccount ? `@${telegramAccount.username || telegramAccount.firstName || "Telegram user"}` : "Not connected"}</div></div></div><div className="flex gap-2"><button disabled={busy} onClick={() => void reconnectTelegram()} className="rounded-lg border border-sky-300 px-3 py-2 text-[10px] font-bold text-sky-600 dark:border-sky-400/40 dark:text-sky-300">{telegramAccount ? "Relink" : "Connect Telegram"}</button>{telegramAccount && <button disabled={busy} onClick={() => void disconnectTelegram()} className="rounded-lg border border-red-300 px-3 py-2 text-[10px] font-bold text-red-500 dark:border-red-400/30">Unlink</button>}</div></div></div></div>
      <div className="mt-5 rounded-xl border border-slate-200 bg-white p-5 shadow-[0_2px_10px_rgba(15,23,42,0.04)] dark:border-white/10 dark:bg-[#0d0c11]"><div className="flex items-center justify-between"><div><h2 className="text-sm font-bold">Wallets</h2><p className="mt-1 text-[10px] text-slate-500 dark:text-zinc-500">Add multiple wallets to verify token ownership. All of these wallets can also access your profile.</p></div><button onClick={() => { setError(""); setAddress(""); setChainModalOpen(true); }} className="rounded-lg bg-violet-600 px-4 py-2 text-[10px] font-bold text-white">Add wallet +</button></div><div className="mt-5 border-t border-slate-100 pt-4 dark:border-white/10"><h3 className="text-[10px] font-bold">Primary wallets</h3><div className="mt-2 grid grid-cols-1 gap-2 sm:grid-cols-2 xl:grid-cols-4">{primaryWallets.length === 0 ? <div className="rounded-lg border border-dashed border-slate-200 p-5 text-center text-[10px] text-slate-400 dark:border-white/10 dark:text-zinc-500 sm:col-span-2 xl:col-span-4">No primary wallets yet.</div> : primaryWallets.map((wallet) => { const chain = chainForWallet(wallet); return <div key={wallet.id} className="rounded-xl border border-slate-200 bg-white p-3 shadow-[0_2px_8px_rgba(15,23,42,0.04)] dark:border-white/10 dark:bg-black/20"><div className="flex items-center justify-between gap-2"><div className="flex min-w-0 items-center gap-2">{chain && <img src={chain.icon} alt="" className="h-5 w-5" />}<span className="truncate text-[10px] font-bold">{chain?.name ?? wallet.network}</span></div><span className="rounded-full bg-sky-500 px-2 py-1 text-[8px] font-bold text-white">● Primary</span></div><div className="mt-3 rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 font-mono text-[10px] text-slate-600 dark:border-white/10 dark:bg-[#0a0a0e] dark:text-zinc-400">{shortenAddress(wallet.address)}</div><div className="mt-2 text-right"><button onClick={() => void removeWallet(wallet.id)} className="text-[9px] font-bold text-red-500">Remove</button></div></div>; })}</div></div><div className="mt-6 border-t border-slate-100 pt-4 dark:border-white/10"><h3 className="text-[10px] font-bold">Other wallets</h3><div className="mt-2 grid grid-cols-1 gap-2 sm:grid-cols-2 xl:grid-cols-4">{otherWallets.length === 0 ? <div className="rounded-lg border border-dashed border-slate-200 p-5 text-center text-[10px] text-slate-400 dark:border-white/10 dark:text-zinc-500 sm:col-span-2 xl:col-span-4">No other wallets.</div> : otherWallets.map((wallet) => { const chain = chainForWallet(wallet); return <div key={wallet.id} className="rounded-xl border border-slate-200 bg-white p-3 dark:border-white/10 dark:bg-black/20"><div className="flex items-center justify-between gap-2"><div className="flex min-w-0 items-center gap-2">{chain && <img src={chain.icon} alt="" className="h-5 w-5" />}<span className="truncate text-[10px] font-bold">{chain?.name ?? wallet.network}</span></div><button onClick={() => void makePrimary(wallet.id)} className="rounded-full bg-violet-600 px-2 py-1 text-[8px] font-bold text-white">Make Primary</button></div><div className="mt-3 rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 font-mono text-[10px] text-slate-600 dark:border-white/10 dark:bg-[#0a0a0e] dark:text-zinc-400">{shortenAddress(wallet.address)}</div><div className="mt-2 text-right"><button onClick={() => void removeWallet(wallet.id)} className="text-[9px] font-bold text-red-500">Remove</button></div></div>; })}</div></div></div>
      <div className="mt-5 grid gap-5 md:grid-cols-2"><div className="rounded-xl border border-slate-200 bg-white p-5 dark:border-white/10 dark:bg-[#0d0c11]"><h2 className="text-sm font-bold">Notifications</h2><p className="mt-1 text-[10px] text-slate-500 dark:text-zinc-500">Notification preferences are managed by the current account system.</p></div><div className="rounded-xl border border-slate-200 bg-white p-5 dark:border-white/10 dark:bg-[#0d0c11]"><h2 className="text-sm font-bold">Activity</h2><p className="mt-1 text-[10px] text-slate-500 dark:text-zinc-500">Account activity will appear here when the activity API is enabled.</p></div><div className="rounded-xl border border-slate-200 bg-white p-5 dark:border-white/10 dark:bg-[#0d0c11]"><h2 className="text-sm font-bold">Referrals</h2><p className="mt-1 text-[10px] text-slate-500 dark:text-zinc-500">Referral tools are ready to be connected to the referral backend.</p></div><div className="rounded-xl border border-slate-200 bg-white p-5 dark:border-white/10 dark:bg-[#0d0c11]"><h2 className="text-sm font-bold">API Keys</h2><p className="mt-1 text-[10px] text-slate-500 dark:text-zinc-500">API key management is not exposed by the current account API.</p></div></div><div className="mt-8 border-t border-slate-200 pt-5 text-[9px] text-slate-400 dark:border-white/10"><Link href="/" className="font-bold text-violet-600">Raven Oracle</Link> · Account settings</div>
    </section></div>
    {chainModalOpen && <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/50 p-4 backdrop-blur-sm" onMouseDown={(event) => { if (event.target === event.currentTarget) setChainModalOpen(false); }}><div className="flex max-h-[90vh] w-full max-w-5xl flex-col overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-2xl dark:border-white/10 dark:bg-[#0d0c11]"><div className="flex items-center justify-between border-b border-slate-200 px-5 py-4 dark:border-white/10"><div><h2 className="text-base font-bold">Add New Wallet</h2><p className="mt-1 text-[10px] text-slate-500 dark:text-zinc-500">Select a chain, enter its compatible public address, then save it.</p></div><button onClick={() => setChainModalOpen(false)} className="rounded-lg border border-slate-200 px-3 py-2 text-xs text-slate-500 dark:border-white/10 dark:text-zinc-400">✕</button></div><div className="overflow-y-auto p-5"><div className="grid grid-cols-2 gap-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5">{CHAIN_OPTIONS.map((chain) => <button key={chain.id} onClick={() => { setSelectedChainId(chain.id); setAddress(""); setError(""); }} className={`flex items-center gap-3 rounded-xl border px-3 py-3 text-left transition ${selectedChainId === chain.id ? "border-violet-500 bg-violet-50 ring-2 ring-violet-100 dark:bg-violet-500/10 dark:ring-violet-500/20" : "border-slate-200 bg-white hover:border-violet-200 hover:bg-slate-50 dark:border-white/10 dark:bg-black/20 dark:hover:bg-white/5"}`}><img src={chain.icon} alt="" className="h-7 w-7 shrink-0" /><span className="truncate text-[10px] font-bold text-slate-700 dark:text-zinc-200">{chain.name}</span></button>)}</div><div className="mt-5 rounded-xl border border-slate-200 bg-slate-50 p-4 dark:border-white/10 dark:bg-black/20"><div className="flex items-center gap-3"><img src={selectedChain.icon} alt="" className="h-9 w-9" /><div><div className="text-xs font-bold">{selectedChain.name}</div><div className="text-[9px] text-slate-500 dark:text-zinc-500">{selectedChain.family} address</div></div></div><div className="mt-3 flex flex-col gap-2 sm:flex-row"><input autoFocus value={address} onChange={(event) => setAddress(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter") void addWallet(); }} placeholder={addressPlaceholder(selectedChain)} className="min-w-0 flex-1 rounded-lg border border-slate-200 bg-white px-3 py-3 text-xs font-mono outline-none focus:border-violet-400 focus:ring-2 focus:ring-violet-100 dark:border-white/10 dark:bg-[#08080b]" /><button disabled={busy} onClick={() => void addWallet()} className="rounded-lg bg-violet-600 px-6 py-3 text-xs font-bold text-white disabled:opacity-50">{busy ? "Saving…" : "Save Wallet"}</button></div><p className="mt-2 text-[9px] text-slate-400">Only public wallet addresses are accepted. Never enter a private key or seed phrase.</p></div></div></div></div>}
    {telegramOpen && <div className="fixed inset-0 z-[60] flex items-center justify-center bg-slate-950/50 p-4 backdrop-blur-sm"><div className="w-full max-w-md rounded-2xl border border-slate-200 bg-white p-6 shadow-2xl dark:border-white/10 dark:bg-[#0d0c11]"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-full bg-sky-50 dark:bg-sky-500/10"><SocialIcon provider="TELEGRAM" /></div><div><h2 className="text-sm font-bold">Connect Telegram</h2><p className="text-[10px] text-slate-500 dark:text-zinc-500">Link your Telegram account to Raven Oracle</p></div></div>{telegramAccount ? <><div className="mt-5 rounded-xl border border-emerald-200 bg-emerald-50 p-4 dark:border-emerald-900/40 dark:bg-emerald-950/20"><div className="text-[10px] font-bold text-emerald-700 dark:text-emerald-300">Telegram connected</div><div className="mt-1 text-xs text-emerald-800 dark:text-emerald-200">{telegramAccount.username ? `@${telegramAccount.username}` : telegramAccount.firstName || "Telegram user"}</div><div className="mt-1 font-mono text-[9px] text-emerald-700 dark:text-emerald-300">ID: {telegramAccount.telegramUserId}</div></div><div className="mt-5 flex justify-end gap-2"><button onClick={() => setTelegramOpen(false)} className="rounded-lg border border-slate-200 px-4 py-2 text-[10px] font-bold text-slate-600 dark:border-white/10 dark:text-zinc-300">Close</button><button disabled={busy} onClick={() => void reconnectTelegram()} className="rounded-lg border border-sky-300 px-4 py-2 text-[10px] font-bold text-sky-600 dark:border-sky-400/40 dark:text-sky-300">Relink</button><button disabled={busy} onClick={() => void disconnectTelegram()} className="rounded-lg border border-red-300 px-4 py-2 text-[10px] font-bold text-red-500 dark:border-red-400/30">Unlink</button></div></> : <><p className="mt-5 text-xs leading-5 text-slate-600 dark:text-zinc-400">Click below to open the Raven Oracle Telegram bot. Press <span className="font-bold">Start</span> in Telegram to complete the connection, then return to your profile.</p><div className="mt-5 flex justify-end gap-2"><button onClick={() => setTelegramOpen(false)} className="rounded-lg border border-slate-200 px-4 py-2 text-[10px] font-bold text-slate-600 dark:border-white/10 dark:text-zinc-300">Cancel</button><button disabled={busy} onClick={() => void connectTelegram()} className="rounded-lg bg-sky-500 px-4 py-2 text-[10px] font-bold text-white disabled:opacity-50">{busy ? "Opening…" : "Open Telegram"}</button></div></>}</div></div>}
  </main>;
}
